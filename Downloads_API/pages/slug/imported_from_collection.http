#!/usr/bin/env dothttp

// Returns a JSON hash of all download files for a given product page.

@name("List downloadable files for a product")
GET "https://{{baseUrl}}/pages/:slug"


// Downloads a requested file.

@name("Download a file")
GET "https://{{baseUrl}}/pages/:slug/files/:file"



