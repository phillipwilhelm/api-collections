The goal of this collection is to share insights into how we can build and execute requests in async format, by chaining multiple requests under a single pre-request or test script.

It's mostly useful when you want to run a lot of pre-requests before the actual request is made and also in a specific fashion.