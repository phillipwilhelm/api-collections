The `Visualizer` feature allows response data to be displayed in a number of different ways. Pulling in external modules expands the capability even further.

This Collection demonstrates how you can use the [jsonpath](https://www.npmjs.com/package/jsonpath)  package, in the Visualize tab, to filter the response JSON data returned in your requests.