#!/usr/bin/env dothttp

// Return counts of distinct wireless clients connecting to a network over time

@name("Return counts of distinct wireless clients connecting to a network over time")
GET "https://{{baseUrl}}/networks/:networkId/wireless/clients/onboardingHistory"
"X-Cisco-Meraki-API-Key": ""
? "t0"= "{{t0}}"
? "t1"= "{{t1}}"
? "timespan"= "{{timespan}}"
? "resolution"= "{{resolution}}"
? "band"= "{{band}}"
? "ssid"= "{{ssid}}"



