#!/usr/bin/env dothttp

@name("")
GET "http://test.fhir.org/r4/Subscription"
"Accept": "application/json"
? "_content"= "{{_content}}"
? "_id"= "{{_id}}"
? "_lastUpdated"= "{{_lastUpdated}}"
? "_profile"= "{{_profile}}"
? "_query"= "{{_query}}"
? "_security"= "{{_security}}"
? "_tag"= "{{_tag}}"
? "_text"= "{{_text}}"
? "contact"= "{{contact}}"
? "criteria"= "{{criteria}}"
? "payload"= "{{payload}}"
? "status"= "{{status}}"
? "tag"= "{{tag}}"
? "type"= "{{type}}"
? "url"= "{{url}}"
? "_sort"= "{{_sort}}"
? "_count"= "{{_count}}"
? "_summary"= "{{_summary}}"
? "_include"= "{{_include}}"
? "_reverseInclude"= "{{_reverseInclude}}"
? "_filter"= "{{_filter}}"
? "_format"= "{{_format}}"


@name("")
POST "http://test.fhir.org/r4/Subscription"
"Accept": "application/json"



@name("")
GET "http://test.fhir.org/r4/Subscription/:id"
"Accept": "application/json"


@name("")
PUT "http://test.fhir.org/r4/Subscription/:id"
"Accept": "application/json"



@name("")
DELETE "http://test.fhir.org/r4/Subscription/:id"
"Accept": "application/json"



@name("")
GET "http://test.fhir.org/r4/Subscription/:id/_history"
"Accept": "application/json"
? "_count"= "{{_count}}"
? "_since"= "{{_since}}"


@name("")
GET "http://test.fhir.org/r4/Subscription/_history"
"Accept": "application/json"
? "_count"= "{{_count}}"
? "_since"= "{{_since}}"


@name("")
GET "http://test.fhir.org/r4/Subscription/:id/_history/:vid"
"Accept": "application/json"



