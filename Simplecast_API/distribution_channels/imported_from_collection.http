#!/usr/bin/env dothttp

// Retrieves all distribution channels.

@name("/distribution_channels")
GET "https://{{host}}/distribution_channels"


// Retrieves a specific distribution channel by its ID.

@name("/distribution_channels/:distribution_channel_id")
GET "https://{{host}}/distribution_channels/{{distribution_channel_id}}"



