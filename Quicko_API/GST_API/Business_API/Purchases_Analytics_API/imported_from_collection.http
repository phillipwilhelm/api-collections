#!/usr/bin/env dothttp

// Provides purchase analysis

@name("Get Purchases Analytics")
GET "https://{{host}}/gst/businesses/:business_id/purchases"
"Authorization": "{{access_token}}"
"x-api-key": "{{api_key}}"
"x-api-version": "{{version}}"
? "query"= "{{query}}"
? "aggregation"= "{{aggregation}}"
? "from"= "{{from}}"
? "to"= "{{to}}"



