#!/usr/bin/env dothttp

// Provides sales analysis

@name("Get Sales Analytics")
GET "https://{{host}}/gst/businesses/:business_id/sales"
"Authorization": "{{access_token}}"
"x-api-key": "{{api_key}}"
"x-api-version": "{{version}}"
? "query"= "{{query}}"
? "aggregation"= "{{aggregation}}"
? "from"= "{{from}}"
? "to"= "{{to}}"



