#!/usr/bin/env dothttp

// Provides payment analysis

@name("Get Payments Analytics")
GET "https://{{host}}/tds/payers/:payer_id/analytics/payments"
"Authorization": "{{access_token}}"
"x-api-key": "{{api_key}}"
"x-api-version": "{{version}}"
? "query"= "{{query}}"
? "aggregation"= "{{aggregation}}"
? "from"= "{{from}}"
? "to"= "{{to}}"



