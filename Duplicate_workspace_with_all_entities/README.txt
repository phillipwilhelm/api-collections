This collection can be used to duplicate a workspace with all entities (collections, environments, mocks and monitors). Uses the Postman API to achieve the same

This will need 3 environment variables -> `apiKey`, `apiUrl` and `workspaceId` (id of the workspace to be duplicated)