#!/usr/bin/env dothttp

// Get application health by time

@name("Get application health by time")
GET "https://{{baseUrl}}/networks/:network_id/insight/applications/:application_id/healthByTime"
"X-Cisco-Meraki-API-Key": ""
? "t0"= "{{t0}}"
? "t1"= "{{t1}}"
? "timespan"= "{{timespan}}"
? "resolution"= "{{resolution}}"



