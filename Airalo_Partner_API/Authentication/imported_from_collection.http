#!/usr/bin/env dothttp

// Request Access Token.

@name("Request Access Token")
POST "https://{{url}}/{{version}}/token"
"Accept": "application/json"
files(
	("client_id", "CLIENT_ID"),
	("client_secret", "CLIENT_SECRET"),
	("grant_type", "client_credentials")
)



