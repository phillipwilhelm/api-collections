#!/usr/bin/env dothttp

// Returns a full list of store IDs, names, and a flag specifying if store is active.// // Response also includes an collection of banner / logo / icon image URLs for each store. The URLs are all relative format, for example - // // `https://www.cheapshark.com/<image-url-here>`
@name("Stores Info")
GET "https://{{host}}/api/1.0/stores"




