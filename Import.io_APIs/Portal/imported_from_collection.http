#!/usr/bin/env dothttp

// GET Report runs that are public on a portal
// 
// *portalId* - The guid of the user's portal which is generated when the user creates a portal object in the object store.

@name("Get portal report runs")
GET "https://api.import.io/portal/:portalId"




