#!/usr/bin/env dothttp

// Retrrieves the count of users in each state

@name("Consumers status")
GET "https://{{API_BASE_URL}}/metrics/consumers"


// Retrieves metrics about all transactions in the system

@name("Transections status")
GET "https://{{API_BASE_URL}}/metrics/transactions"


// Retrieves the number of wallet in each state in the system.

@name("Wallets status")
GET "https://{{API_BASE_URL}}/metrics/wallets"



