#!/usr/bin/env dothttp

// Haz login en Enconta para obtener un token (auth_token) el cual tendras que utilizar en los Headers con el parametro de Authorization para ejecutar cualquier operación sobre el API.

@name("Login")
POST "https://{{host}}/api/v1/login"
"Content-Type": "application/x-www-form-urlencoded"
data({
    "email": [
        "{{admin}}"
    ],
    "password": [
        "{{password}}"
    ]
})



