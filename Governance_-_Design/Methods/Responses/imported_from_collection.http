#!/usr/bin/env dothttp

// Checks to see if all methods have a 2xx response.
@name("Do all methods have an HTTP Status Success (2xx)?")
GET "https://api.getpostman.com/apis/:api_id/versions/:version_id/schemas/:schema_id"


// Checks to see if all methods have a 5xx response.
@name("Do all methods have an HTTP Status Failure (5xx)?")
GET "https://api.getpostman.com/apis/:api_id/versions/:version_id/schemas/:schema_id"



