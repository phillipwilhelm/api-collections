#!/usr/bin/env dothttp

// This request will generate a JMeter Test Plan from a Postman Collection.  Just provide the `{{collection_uid}}`, hit `Send`, and click `Visualize` in the response!
@name("Generate a JMeter Test Plan")
GET "https://api.getpostman.com/collections/:collection_uid"



