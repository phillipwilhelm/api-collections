#!/usr/bin/env dothttp

// Gives details about a tip, including which users (especially friends) have marked the tip to-do.

@name("Get Details of a Tip")
GET "https://api.foursquare.com/v2/tips/{{TIP_ID}}"
? "client_id"= "{{client_id}}"
? "client_secret"= "{{client_secret}}"
? "v"= "{{v}}"



