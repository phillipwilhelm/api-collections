#!/usr/bin/env dothttp

@name("Retrieve transport connection list from device")
GET "https://{{vmanage}}/dataservice/device/transport/connection"
"X-XSRF-TOKEN": "{{X-XSRF-TOKEN}}"
? "deviceId"= "{{deviceId}}"



