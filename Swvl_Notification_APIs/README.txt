A set of API to create and send send a notification to Swvl users using Push-Notification, SMS, and Email.

In order to send a notification, you have to create a message first and then using the message ID and the user/s ID you can send the notification.

The message POST API create the message with the same language sent "local" and to add translation to the created message send the local with the message ID and translation to the PUT API