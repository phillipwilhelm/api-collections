#!/usr/bin/env dothttp

// to get token we will have to send a POST request with username and password to the /security/login
// 
// If the username and password are right . A Bearer token will be send back To use for future request

@name("(PERMIT ALL)  get token")
POST "https://hvcg-tm.herokuapp.com/security/login"
"Content-Type": "application/json"




