#!/usr/bin/env dothttp

// Gets a specific [application key credential](#application-key-credential-model) by `kid`

@name("Get Key Credential for Application")
GET "https://{{baseUrl}}/api/v1/apps/:appId/credentials/keys/:keyId"


// Clones a X.509 certificate for an application key credential from a source application to target application.

@name("Clone Application Key Credential")
POST "https://{{baseUrl}}/api/v1/apps/:appId/credentials/keys/:keyId/clone"
? "targetAid"= "<string>"



